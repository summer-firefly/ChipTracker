# chipTrack

Android 扑克游戏计分器。当前为工程初始化骨架，可打包 debug APK。

## 环境要求

- JDK 17
- Android SDK（API 34）
- `ANDROID_HOME` 已配置

## 常用命令

```bash
# Debug 包
./gradlew assembleDebug

# 安装到已连接设备
./gradlew installDebug
```

产物路径：`app/build/outputs/apk/debug/app-debug.apk`
